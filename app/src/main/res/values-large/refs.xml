<resources>

    <!--
    Layout alias to replace the single-pane version of the layout with a
    two-pane version on Large screens.

    For more on layout aliases, see:
    http://developer.android.com/training/multiscreen/screensizes.html#TaskUseAliasFilters
    -->
    <item name="fragment_brand" type="layout">@layout/fragment_brand_grid</item>

    <!--
    Layout alias to replace the single-pane version of the layout with a
    two-pane version on Large screens.

    For more on layout aliases, see:
    http://developer.android.com/training/multiscreen/screensizes.html#TaskUseAliasFilters
    -->
    <item name="fragment_category" type="layout">@layout/fragment_category_grid</item>

    <!--
    Layout alias to replace the single-pane version of the layout with a
    two-pane version on Large screens.

    For more on layout aliases, see:
    http://developer.android.com/training/multiscreen/screensizes.html#TaskUseAliasFilters
    -->
    <item name="fragment_statelist" type="layout">@layout/fragment_state_grid</item>
    <item name="fragment_type" type="layout">@layout/fragment_type_grid</item>
    <item name="fragment_citylist" type="layout">@layout/fragment_citylist_grid</item>

    <!--
    Layout alias to replace the single-pane version of the layout with a
    two-pane version on Large screens.

    For more on layout aliases, see:
    http://developer.android.com/training/multiscreen/screensizes.html#TaskUseAliasFilters
    -->
    <item name="fragment_model" type="layout">@layout/fragment_model_grid</item>
    <item name="fragment_highwaystretch" type="layout">@layout/fragment_highwaystretch_grid</item>

</resources>